{
  "name": "@havyn/content-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:production": "wrangler deploy --env production",
    "types": "wrangler types",
    "check": "tsc --noEmit && wrangler deploy --dry-run --outdir .wrangler/dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260713.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.110.0"
  }
}
