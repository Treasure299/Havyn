# Havyn Verify Compatibility Page

This folder exists so the old free Render static service named `havyn-verify` can build successfully if it still exists in Render.

The active Supabase redirect URL is:

```text
https://havyn-web.pages.dev/verify/
```

That route is served by the main Havyn Socket.IO server.
