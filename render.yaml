services:
  - type: web
    name: havyn-socket-server
    runtime: node
    plan: free
    rootDir: apps/server
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: CLIENT_ORIGIN
        value: "*"
  - type: web
    name: havyn-verify
    runtime: static
    buildCommand: echo "Static verification page"
    staticPublishPath: apps/verify
